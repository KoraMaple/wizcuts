#!/usr/bin/env sh
# WizCuts Commit Message Linting
# Ensures commit messages follow conventional commit format

echo "📝 Checking commit message format..."
npx --no -- commitlint --edit $1

echo "✅ Commit message format is valid!"
